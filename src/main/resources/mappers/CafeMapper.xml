<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="kr.or.oti.mapper.CafeMapper">
	<select id="selectAll" resultType="kr.or.oti.domain.CafeVO">
		SELECT
			CAFE_ID AS cafeId,
			CAFE_NAME AS cafeName,
			DESCRIPTION,
			OWNER_USERNAME AS ownerUsername,
			STATUS,
			CREATED_AT AS createdAt,
			UPDATED_AT AS updatedAt
		FROM CAFE
		WHERE STATUS = 'ACTIVE'
		ORDER BY CAFE_ID DESC
	</select>
	
	<select id="selectOne" resultType="kr.or.oti.domain.CafeVO">
		SELECT
			CAFE_ID AS cafeId,
			CAFE_NAME AS cafeName,
			DESCRIPTION,
			OWNER_USERNAME AS ownerUsername,
			STATUS,
			CREATED_AT AS createdAt,
			UPDATED_AT AS updatedAt
		FROM CAFE
		WHERE CAFE_ID = #{cafeId}
			AND STATUS = 'ACTIVE'
	</select>
	
	<insert id="insertOne">
		<selectKey keyProperty="cafeId" resultType="long" order="BEFORE">
			SELECT SEQ_CAFE.NEXTVAL
			FROM DUAL
		</selectKey>
		
		INSERT INTO CAFE
		(
			CAFE_ID,
			CAFE_NAME,
			DESCRIPTION,
			OWNER_USERNAME
		)
		VALUES
		(
			#{cafeId},
			#{cafeName},
			#{description},
			#{ownerUsername}
		)
	</insert>
	
	<update id="updateOne">
		UPDATE CAFE
		SET
			CAFE_NAME = #{cafeName},
			DESCRIPTION = #{description},
			UPDATED_AT = SYSDATE
		WHERE CAFE_ID = #{cafeId}
			AND STATUS = 'ACTIVE'
	</update>
	
	<update id="deleteOne">
		UPDATE CAFE
		SET
			STATUS = 'DELETED',
			UPDATED_AT = SYSDATE
		WHERE CAFE_ID = #{cafeId}
			AND STATUS = 'ACTIVE'
	</update>
</mapper>